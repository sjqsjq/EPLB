# Adaptive Window by Input Length — 实验分析报告 (2026-07-26)

## 实验设计

在 8×H20 单机、Qwen3-235B-A22B-FP8、EP=8/DP=8、deepep-mode=auto、输出=1（纯prefill）条件下，对 Prover-V1 数据集的5个输入长度档（256/512/1024/2048/4096 token）× 4个 sync_window（16/32/64/128）做了完整的网格实验（50次自动化运行，conc=1024）+ 对 L=256 和 L=4096 各做了16次手动交替复核（BL→OE→BL→OE，conc=64/16，排除排队和时间漂移干扰）。

## 核心发现

### 1. OEPLB 收益随输入长度单调递增（主效应）

| 长度 | 最佳window | 该window下 vs BL (网格conc=1024) | 手动复核 vs BL |
|------|-----------|-------------------------------|---------------|
| 256  | sw=16     | +4.0% (高噪声)                | **-6.1%** (负收益) |
| 512  | sw=16     | +12.3%                        | 未测 |
| 1024 | sw=16     | +11.4%                        | 未测 |
| 2048 | sw=16     | +15.2%                        | 未测 |
| 4096 | sw=16     | +17.8%                        | **+15.2%** (确认) |

**手动复核证实**：L=256 的 +4% 是虚假正收益（被高并发度下的排队效应放大），真实值为负；L=4096 的 +15~18% 是真实正收益。

### 2. 不同window之间的差异是次要效应（~4pp），且在短输入下被噪声淹没

L=4096 手动复核（最干净的数据）：

| Window | OE vs BL |
|--------|----------|
| sw=16  | +15.22%  |
| sw=32  | +14.13%  |
| sw=64  | +13.04%  |
| sw=128 | +10.87%  |

趋势方向一致（window越小越好），但差距只有 ~4pp（15.2% - 10.9%），相比 OEPLB 本身 vs baseline 的 +10~15% 差距是次要的。

L=256 手动复核：baseline自身方差（±4%）已经完全盖过了window之间的差异，无法可靠区分不同window的效果。

### 3. 机制分析：为什么短输入无收益、长输入有收益

**DIAG日志揭示的核心线索：avg_ratio_before（冷启动不均衡度）随长度下降**

| 长度 | window=16 avg_ratio_before(w1) | 含义 |
|------|-------------------------------|------|
| 256  | 1.747                         | 最不均衡 |
| 512  | 1.761                         | 最不均衡 |
| 1024 | 1.741                         | 略低 |
| 2048 | 1.699                         | 中等 |
| 4096 | 1.634                         | 最低 |

看起来短输入的初始不均衡度反而更高（1.747 vs 1.634），理论上更需要纠偏。**但问题在于短输入的单次prefill耗时太短，冷启动纠偏完成后就没有足够的后续请求来"享受"纠偏带来的收益——2048条×256token的数据集跑完太快（~30秒），而冷启动那一次swap本身就要消耗几百毫秒+后续数个window的all_reduce开销（总计约700-900ms），占整个benchmark时长的~2-3%**。

反过来说，L=4096 的2048条请求跑了~390秒（比L=256的30秒长13倍），同样的冷启动纠偏开销占比降到<0.2%，而纠偏后的"红利"被后续大量请求分摊，净收益自然更高。

**PROF日志印证了这一点：record开销随长度线性增长**

| 长度 | sw=16 record_ms | sw=16 total_overhead_ms | benchmark总时长(s) | 开销占比 |
|------|-----------------|------------------------|--------------------|---------|
| 256  | 393             | 741                    | ~25                | ~3.0%   |
| 512  | 859             | 1278                   | ~47                | ~2.7%   |
| 1024 | 1788            | 2292                   | ~90                | ~2.5%   |
| 2048 | 3792            | 4479                   | ~190               | ~2.4%   |
| 4096 | 7442            | 8107                   | ~390               | ~2.1%   |

record开销随长度增长但benchmark总时长增长更快，所以**开销占比反而随长度下降**——这解释了为什么长输入的净收益更高。

### 4. 不同window的差异机制

DIAG日志显示：所有长度的冷启动第一个window的swap次数几乎相同（231-241次），**真正的差异在于后续的"持续调优"能做多少次**：

| 长度 | sw=16 total_ops | sw=128 total_ops | 差异 |
|------|-----------------|------------------|------|
| 256  | 298             | 0 (未触发)       | sw=128连一次DIAG都没触发 |
| 512  | 311             | 235              | sw=128只触发了1次 |
| 1024 | 533             | 302              | 差异明显 |
| 4096 | 378             | 305              | 差异缩小 |

短输入下sw=128几乎来不及做第二次检查（L=256时sw=128的DIAG窗口数为0），大window会错过纠偏机会。但在L=4096这种长请求下，即使sw=128也能触发4次DIAG窗口做305次swap——这解释了为什么L=4096下不同window的差异收窄到~4pp（因为冷启动那一次231-241次swap已经修正了大部分不均衡，后续的边际swap带来的增量收益有限）。

## 对"adaptive window by length"方向的评估

基于以上分析，**按输入长度动态调整sync_window这个方向的价值有限**，原因：

1. **不同window的收益差异（~4pp）远小于OEPLB本身vs baseline的收益（+10~15%）**——意味着哪怕window选的不是最优的，也不会显著影响OEPLB的净收益
2. **在短输入场景下OEPLB本身就没有正收益**——不是因为window选错了，而是因为benchmark太短、开销占比太高、纠偏红利来不及分摊。调window解决不了这个根本问题
3. **在长输入场景下所有window都给出了+10~15%的正收益**——默认sw=64已经足够好（+13%），没有强烈的需求去优化到sw=16的+15%

### 更有价值的方向

与其按长度调window，不如优化以下两个方面：

1. **降低短输入场景下的固定开销**：record_next_layer的per-call开销（短输入下每call ~0.4ms × 94层 × 大量forward pass）是主要成本。如果能在检测到短输入时自动降低采样频率（sample_interval调大）或完全跳过record，就能消除短输入下的负收益
2. **保留现有的domain-shift adaptive window**：FINAL_235B_REPORT.md已验证这个机制在domain切换场景有+7~11%的真实正收益——这是一个不同于"按长度调window"的、独立的、已验证有效的优化方向

## 建议

1. 保留现有代码里的 `adaptive_window`（基于cos_sim检测domain shift），它已被验证有效
2. 不实现"按输入长度动态调window"——收益不显著，不值得增加代码复杂度
3. 考虑实现"按输入长度自适应采样频率"——在短输入（<512 token）场景下增大 `sample_interval` 或完全跳过 record，消除短输入下的负收益
