# 负载探究实验报告：输入/输出长度 3×3 网格 (2026-07-21~22)

## 目的

探究输入长度、输出长度分别对 OEPLB 收益的影响，为后续 adaptive 参数（decay/window/threshold等）调整提供依据。

## 数据集设计

**DeepSeek-Prover-V1**（全量27503条Lean4证明，用Qwen3-235B tokenizer实测分布后分桶）：

| | 输入长度(token) | 桶内均值 | 输出长度(max_tokens) |
|---|---|---|---|
| short | 90-180 | 153.9 | 8 |
| medium | 200-260 | 227.7 | 64 |
| long | 400-700 | 493.5 | 200 |

每个输入桶取2048条唯一prompt，配3种输出长度各生成一份，共9个jsonl（`OEPLB/benchmarks/workload_grid/`）。

## 测试方法

起1次baseline server连跑9种负载，起1次OEPLB server（原版静态sync_window=64,未开adaptive_window/adaptive_decay）连跑9种负载，重复3轮（3次baseline boot + 3次OEPLB boot）。每轮里9个负载共享同一个server进程（不重启），负载之间用timestamp标记边界方便事后切片分析日志。

## 结果：3轮均值

| 负载(输入_输出) | BL req/s | OE req/s | req/s提升 | BL TTFT | OE TTFT | TTFT变化 | BL TPOT | OE TPOT | TPOT变化 |
|---|---|---|---|---|---|---|---|---|---|
| short_short | 121.56 | 135.56 | +11.5% | 4366 | 3861 | -11.6% | 568.7 | 517.3 | -9.0% |
| short_medium | 79.96 | 87.24 | +9.1% | 4198 | 3636 | -13.4% | 135.4 | 127.5 | -5.9% |
| short_long | 43.35 | 45.59 | +5.2% | 4396 | 3815 | -13.2% | 96.2 | 93.3 | -3.0% |
| medium_short | 85.58 | 97.03 | +13.4% | 6020 | 5262 | -12.6% | 838.2 | 744.4 | -11.2% |
| medium_medium | 62.31 | 69.63 | +11.8% | 6058 | 5103 | -15.8% | 163.6 | 151.2 | -7.5% |
| medium_long | 37.46 | 40.51 | +8.1% | 5946 | 5044 | -15.2% | 107.1 | 101.3 | -5.4% |
| long_short | 41.09 | 47.28 | +15.1% | 12400 | 10682 | -13.9% | 1776.1 | 1554.9 | -12.5% |
| long_medium | 34.19 | 38.96 | +13.9% | 12511 | 10598 | -15.3% | 275.4 | 247.7 | -10.0% |
| long_long | 24.50 | 27.03 | +10.3% | 12464 | 10505 | -15.7% | 147.0 | 137.1 | -6.7% |

**全部9种负载，OEPLB在req/s、TTFT、TPOT三个指标上都是同时改善的，没有出现负收益的组合。**

## 趋势1：输入长度越长，req/s 收益越大

固定输出长度看输入维度：short输入(+5.2%~+11.5%) < medium输入(+8.1%~+13.4%) < long输入(+8.1%~+15.1%，其中long_short达到本次实验最高的+15.1%)。符合直觉——输入越长，prefill阶段在总时长里占比越大，专家负载不均衡对prefill的影响也越大，OEPLB主要靠纠正prefill阶段的负载不均衡获益，输入越长可纠正的空间也越大。

## 趋势2：输出长度越长，TPOT改善幅度越小（但req/s收益方向不受影响）

固定输入长度看输出维度，TPOT的改善幅度单调递减：
- short输入：-9.0%(short输出) → -5.9%(medium) → -3.0%(long)
- medium输入：-11.2% → -7.5% → -5.4%
- long输入：-12.5% → -10.0% → -6.7%

## 关于"decode是否受OEPLB影响"的验证：受影响，且是真实的因果关系

OEPLB的检测/决策机制100%基于prefill数据（`controller.py`里`record_next_layer`只在`is_prefill`时记录，decode batch完全不参与判断"要不要swap"）。但**TPOT(decode阶段单token耗时)在全部9种负载里都有实测改善(-3.0%~-12.5%)**，这不是矛盾——检测是prefill-only的，但swap改变的是`physical_to_logical_map`(专家物理位置)这个全局共享状态，之后**所有forward pass(不管prefill还是decode)都用这个新placement**，所以decode步的MoE计算一样会因为专家分布更均衡而受益。**检测范围和效果范围是两件不同的事,不能因为"只用prefill数据做决策"就推断"decode不受影响"。**

## 深挖："为什么长输出场景下TPOT改善幅度更小"——源码级根因分析

### 现象：链式跑法下,long_long阶段的swap决策窗口数明显偏少

把 `long_long`(输出1024,来自后续BookCorpus场景的探索,非本网格)这一段在链式9负载测试里的耗时窗口单独切出来看`PB-OEPLB-DIAG`日志：81秒里只有2个决策窗口(swap ops=2, 6),而同样输入桶的`long_short`(输出32)50秒里有3个窗口(ops=34,26,12)。乘除一下，long_long阶段每秒的swap密度远低于long_short。

### 根因(有源码+日志双重证据)

**1. SGLang scheduler侧**(`scheduler.py::get_new_batch_prefill`)：
```python
if (self.running_batch.batch_is_full or len(self.waiting_queue) == 0) and self.chunked_req is None:
    return None
```
只要等待队列`waiting_queue`是空的,scheduler根本不会尝试组装新的prefill batch——跟服务器还有多少容量无关。

**2. 客户端侧**(`long_bench.py`)：
```python
done, active = await asyncio.wait(active, return_when=asyncio.FIRST_COMPLETED)
```
`FIRST_COMPLETED`返回时`done`包含**所有**已完成的任务,不只是刚好完成的那一个。由于同一个workload里所有请求的`max_tokens`完全相同,SGLang连续批处理逐token同步推进所有序列,同一批进来的请求几乎会在同一个decode step一起打满max_tokens、一起结束——形成"一大批同时完成→一大批同时发出新请求"的波浪式模式,而不是平滑的trickle补位。

原始log实测：81秒的long_long窗口里,`t+26s`到`t+44s`之间有整整18秒`queue=0, running=127`纹丝不动(完全没有新prefill),然后突然爆发式涌入新请求。

### 用jitter修复客户端——结果是负面的,原假设被推翻

基于上述分析,设计了`long_bench_jitter.py`：给每个请求的`max_tokens`按±15%随机抖动(固定种子,baseline/OEPLB看到完全相同的抖动序列,保证公平对比),打散同步完成模式。

**验证方法**：全新起服务器(不链式,排除"继承自上一个workload的过渡态"这个混杂因素)，同一个`long_long`负载(workload_grid/long_long.jsonl, 输出200),原始客户端 vs jitter客户端各跑2轮。

| | req/s | TPOT(ms) | 决策窗口数(单轮) |
|---|---|---|---|
| 原始客户端(2轮：26.94,26.97) | 26.95 | 136.22 | 7个 (240,44,3,59~64,2~6,1~2,2) |
| Jitter客户端(2轮：24.77,24.35) | **24.56** | **171.75** | 5-6个 (240,44,55~58,7~8,1~2,[1]) |
| **Delta** | **-8.87%** | **+26.09%（明显更差）** | 窗口数没有显著增加 |

**两个意外发现，推翻了之前的假设**：

1. **全新起服务器(标准对照,非链式)跑`long_long`,原始客户端本来就有7个决策窗口**,跟之前在9负载链式测试里观察到的"只有2个窗口"完全不一样——说明之前诊断的"沉默期饥饿"现象,很可能是**链式测试方法论本身的边界效应**(继承自紧邻的上一个workload`long_medium`退出时的过渡态,不是`long_long`单独运行时的普遍规律),不能一概而论到"所有长输出场景都会饿死controller"。
2. **Jitter没有带来预期的收益,反而让req/s和TPOT都明显变差**（req/s -8.87%,TPOT +26.09%,两轮结果一致,不是噪声）。即便决策窗口数量没有明显增加,吞吐反而下降——推测jitter打散完成时间后,原本整齐同步完成的一批解码请求变成参差不齐的完成节奏,这**对decode batch本身的调度/批处理效率是负面的**(比如无法维持一个干净、大小整齐的CUDA graph batch,需要更频繁地插入小规模的prefill补位),这个代价超过了"多检测几个窗口"能带来的收益。

**结论：这次针对"长输出让swap利用不充分"的client-side jitter修复方案，实测是net negative，不采用。** 之前诊断出的"18秒沉默期"是真实存在的现象(源码+日志都证实了机制),但它更可能是**链式多负载测试方法论的产物**，而不是"长输出"这个变量本身内在的、需要被修复的缺陷——standalone跑长输出时OEPLB本来就工作正常。如果之后还想继续优化"长输出下swap利用率"这个方向，下一步应该先确认"链式边界效应"到底有多大影响(比如给每个workload之间加一个drain期,等前一个workload完全跑完再进入下一个),而不是急着在客户端加jitter这种改变了workload本身语义的修复。

## 已知局限

1. 本次分桶只覆盖了DeepSeek-Prover-V1这一个数据集，输入长度上限只到700 token（受限于该数据集本身分布，>700 token的样本很少）。BookCorpus那组的分桶（>4k token输入/输出）实验因为发现了上述方法论问题被暂停，未完成。
2. TPOT/req/s趋势判断都是3轮均值，部分组合的标准差还需要检查是否会影响趋势判断的稳健性（未在本报告展开逐组的std数据，可从`/tmp/grid_results/*.log`原始日志复核）。
3. jitter实验只做了2轮，n=2的置信度有限，虽然两轮方向高度一致(rps -8.87%/tpot+26%的量级重复出现)，但更严格的结论还需要更多轮次验证。

---

# BookCorpus 网格实验 (2026-07-21~22)

## 数据集设计

用 Qwen3-235B tokenizer 实测确定分桶（长输入通过**拼接2本不同书的节选**凑到目标长度，按用户要求）：

| | 输入长度(token) | 桶内均值 | 构造方式 | 输出长度(max_tokens) |
|---|---|---|---|---|
| short | ~800 | 841.6 | 单本书截取1段 | 32 |
| medium | ~3000 | 3040.5 | 单本书截取1段(更长) | 256 |
| long | ~5000 | 5041.5 | **拼接2本不同书各一段** | 1024 |

每桶2048条唯一prompt，来自2048本不同的BookCorpus小说，共9个jsonl（`OEPLB/benchmarks/bookcorpus_grid/`）。

## 前情：客户端jitter修复方案的验证（负面结果，未采用于本次网格）

在跑BookCorpus之前，针对"长输出场景下OEPLB检测器可能被同步完成模式饿死"这个猜测，设计了`long_bench_jitter.py`（给每个请求`max_tokens`按±15%随机抖动，固定种子保证baseline/OEPLB看到相同抖动序列），在Prover-V1的`long_long`负载上做了独立验证（全新起服务器，非链式，2轮）：

| | req/s | TPOT(ms) | 单轮决策窗口数 |
|---|---|---|---|
| 原始客户端 | 26.95 | 136.22 | 7个 |
| Jitter客户端 | 24.56 | 171.75 | 5-6个 |
| Delta | **-8.87%** | **+26.09%（更差）** | 没有明显增加 |

**结论：jitter方案是net negative，不采用。** 而且发现一个重要的反常现象：**全新起服务器（非链式）跑`long_long`，原始客户端本来就有7个决策窗口**，跟之前在9负载链式测试里观察到的"只有2个窗口"完全不同——说明之前诊断的"沉默期饥饿"更可能是**链式多负载测试方法论本身的边界效应**（继承自紧邻上一个workload退出时的过渡态），不是"长输出"这个变量内在的、必须修复的缺陷。因此本次BookCorpus网格沿用原始（非jitter）客户端方法论，跟Prover-V1网格保持一致。

## 测试方法

跟Prover-V1网格相同：起1次baseline server连跑9种负载，起1次OEPLB server连跑9种负载。**本次只完成了1轮（repeat1）**——原因见下方"发现的方法论缺陷"。

## Repeat1 完整结果

| 负载(输入_输出) | BL耗时(s) | BL错误数 | OE耗时(s) | OE错误数 | 耗时变化 | BL TTFT(ms) | OE TTFT(ms) | TTFT变化 | BL TPOT(ms) | OE TPOT(ms) | TPOT变化 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| short_short | 89.8 | 0 | 82.0 | 0 | -8.7% | 21214 | 19546 | -7.9% | 762.3 | 688.5 | -9.7% |
| short_medium | 133.2 | 0 | 119.3 | 0 | -10.4% | 20966 | 17900 | -14.6% | 178.5 | 163.5 | -8.5% |
| short_long | 341.3 | 0 | 327.6 | 0 | -4.0% | 24234 | 20856 | -13.9% | 128.6 | 125.3 | -2.6% |
| medium_short | 344.4 | 0 | 297.5 | 0 | -13.6% | 100311 | 86364 | -13.9% | 1544.5 | 1350.1 | -12.6% |
| medium_medium | 439.3 | 0 | 382.9 | 0 | -12.8% | 117588 | 101325 | -13.8% | 292.2 | 258.2 | -11.6% |
| medium_long | 789.1 | 0 | 725.6 | 0 | -8.1% | 182856 | 164398 | -10.1% | 145.5 | 136.2 | -6.4% |
| long_short | 617.7 | 0 | 531.9 | 0 | -13.9% | 203521 | 174706 | -14.2% | 1703.8 | 1457.0 | -14.5% |
| long_medium | 765.8 | 0 | 682.3 | 0 | -10.9% | 240648 | 212762 | -11.6% | 304.6 | 275.7 | -9.5% |
| long_long | 1164.0 | **144** | 1111.2 | **197** | -4.5% | 330584 | 300882 | -9.0% | 131.0 | 133.0 | **+1.5%（唯一负收益,量级很小,可能是噪声)** |

**8/9种负载,OEPLB在处理总耗时、TTFT、TPOT三个指标上都同时改善,幅度普遍比Prover-V1网格更大(耗时-4%~-14%,TTFT-8%~-15%,TPOT-2.6%~-14.5%)**——BookCorpus的输入普遍比Prover-V1长(最短的输入桶都有800token,超过Prover-V1的long桶),跟"输入越长收益越大"的趋势吻合。

`long_long`这一格的TPOT出现了唯一的、幅度很小的负值(+1.5%)，但这一格也是唯一出现大量请求超时错误的（见下）,两边(BL/OE)完成的具体请求子集不完全一样,这个±1.5%量级的差异更可能是错误子集不同造成的统计噪声,不是真实的负收益,不需要过度解读。

## 发现的方法论缺陷：`long_long`（5000tok输入 + 1024tok输出）触发大量600秒客户端超时

`long_long`这一格,baseline和OEPLB**都**出现了两位数的请求错误(baseline 144个/7.0%,OEPLB 197个/9.6%)——TTFT均值奔到30-33万毫秒(5-5.5分钟!)，加上p99 TTFT接近45-59万毫秒,叠加1024个token的decode时间,总耗时逼近甚至超过`long_bench.py`里`aiohttp.ClientTimeout(total=600)`这个600秒硬上限,一部分请求被客户端主动掐断记为error。

**这不是OEPLB或baseline哪一方的缺陷,是当前`N=2048, conc=1024`这个测试规格,配合`长输入+长输出`这个组合本身，对235B模型这个部署规模来说吞吐爬升太慢，超过了600秒的客户端超时容忍度。** 两边都受影响(甚至OEPLB受影响还略重,197>144,但这个差异也可能是噪声,总耗时OE其实还比BL快4.5%,只是错误率恰好高了一点,值得后续留意但不能就此说OEPLB在这个格子上更差)。

**因此，本次BookCorpus网格实验在repeat1完成后停止，不再继续repeat2/3**，原因：
1. 已经观察到的方法论缺陷（超时错误）会在每一轮`long_long`格子上重复出现，不修复的话3轮跑出来的`long_long`数据都是带瑕疵的，多跑没有意义。
2. 单轮repeat（18个负载,baseline9个+OEPLB9个）实测耗时约**5-6小时**，比Prover-V1网格(约40分钟/轮)慢了一个数量级——BookCorpus的输入长度（800-5000 token）比Prover-V1（90-700 token）大得多，`long_long`单格就要跑将近20分钟。3轮的时间成本(15-18小时)在没解决超时问题之前不划算。

**如果要继续,下一步应该先修复超时问题**，比如：把`long_bench.py`的`aiohttp.ClientTimeout(total=600)`调大(比如1200-1800秒)以完整覆盖`long_long`这种重负载,或者把`long_long`格子的`N`从2048降到512-1024以缩短总耗时,而不是无脑重跑3轮背着同样的错误率。

## 跟Prover-V1网格趋势对比小结

两个数据集的核心结论一致：**输入越长,OEPLB的收益(不管是req/s/耗时还是TTFT)越大；输出越长,TPOT的改善幅度会变小（但不会变成负收益，除了long_long那个被超时污染的格子)**。BookCorpus由于输入普遍比Prover-V1长,整体收益幅度也普遍更大,进一步印证了"输入长度是驱动OEPLB收益大小的主变量"这个判断。

---

# 补充：Swap次数与Ratio记录 (2026-07-22)

之前的表格漏记了swap次数和不均衡度(ratio)的逐负载记录，补上。**以后所有实验都要带上这部分记录。**

## Prover-V1 网格：OEPLB侧每个负载的swap记录（3轮）

用`driver.log`里每个负载精确的起止时间戳(epoch秒)去切`server_oeplb_r{1,2,3}.log`的`PB-OEPLB-DIAG`行。baseline没有OEPLB,不产生这类日志,只统计OEPLB侧。

| 负载 | r1总swap | r2总swap | r3总swap | r1窗口数 | r2窗口数 | r3窗口数 | avg_ratio_before范围(3轮合并) |
|---|---|---|---|---|---|---|---|
| short_short | 404 | 160 | 397 | 4 | 3 | 4 | 1.183-1.711 |
| short_medium | 0 | 1 | 2 | 0 | 1 | 1 | 1.181-1.183 |
| short_long | 0 | 2 | 0 | 0 | 1 | 0 | 1.181-1.181 |
| medium_short | 23 | 26 | 26 | 2 | 2 | 2 | 1.184-1.196 |
| medium_medium | 3 | 3 | 3 | 2 | 2 | 2 | 1.184-1.189 |
| medium_long | 0 | 0 | 0 | 0 | 0 | 0 | N/A(该窗口内没有触发任何swap决策) |
| long_short | 72 | 57 | 54 | 3 | 2 | 2 | 1.206-1.232 |
| long_medium | 14 | 18 | 18 | 3 | 2 | 3 | 1.207-1.214 |
| long_long | 8 | 8 | 4 | 2 | 1 | 2 | 1.206-1.210 |

**观察**：
1. `short_short`因为是每轮9负载序列的**第一个**,承担了每轮开局那次冷启动大修正(ops经常在100-400+这个量级,r2的160是个例外,可能是那一轮冷启动ratio没那么夸张)，后续负载(继承了已收敛的placement)的swap量普遍小得多(个位数到几十)。
2. `medium_long`三轮都是0——说明在这个输入桶(200-260tok)+这个输出长度(64tok)的组合下,继承来的placement已经足够好,整段时间都没有触发任何新的swap决策，不是bug,是真的没必要swap。
3. `long_short`/`long_medium`/`long_long`(long输入桶)的avg_ratio_before普遍比short/medium输入桶高(1.2+ vs 1.18左右)——**输入越长,继承来的placement在新负载下暴露出的不均衡度也越高**，跟"输入越长收益越大"这个趋势能对应起来：不均衡度更高意味着可纠正空间更大。

## BookCorpus 网格：OEPLB侧每个负载的swap记录（repeat1，仅完成1轮）

用测试日志文件的mtime减去`total_time_s`近似推算每个负载的起止时间(比Prover-V1的精确时间戳粗糙一些,存在几秒误差的可能,但不影响整体趋势判断)。

| 负载 | 总swap | 窗口数 | avg_ratio_before范围 |
|---|---|---|---|
| short_short | **289** | 4 | 1.199-1.715 |
| short_medium | 2 | 2 | 1.205-1.206 |
| short_long | 3 | 2 | 1.202-1.205 |
| medium_short | 56 | 6 | 1.165-1.202 |
| medium_medium | 5 | 2 | 1.198-1.199 |
| medium_long | 4 | 2 | 1.198-1.204 |
| long_short | 3 | 2 | 1.181-1.195 |
| long_medium | 0 | 0 | N/A |
| long_long | 1 | 1 | 1.201-1.201 |

**观察**：
1. 跟Prover-V1网格一样,**冷启动大修正(ops=236, ratio=1.715)全部发生在整个9负载序列的第一格`short_short`**,之后的负载都是在继承已收敛placement的基础上做小幅微调(大多个位数到二十几)。
2. `medium_short`这一格有6个窗口/56次swap,是除首格外swap活动最密集的——可能是因为BookCorpus的medium输入(约3000tok)体量本身就大，配合conc=1024带来大量并发prefill,给了controller更多检测机会。
3. `long_medium`0个窗口，跟Prover-V1网格的`medium_long`类似——不是所有"长输入/中等输出"组合都会触发大量swap,取决于继承来的placement跟新流量分布的实际重合度。

**局限**：BookCorpus这组的时间窗口是用文件mtime近似推算的,精度不如Prover-V1网格(那边是driver脚本记录的精确epoch时间戳),个别窗口的swap计数可能有±1个窗口的边界误差，但总量级的趋势判断不受影响。

---

# 参数优化探索 (2026-07-22)

## 方案

选3个代表性负载(`long_short`收益最大、`short_short`收益最小、`long_long`decode最重)，对比3组参数配置，在Prover-V1上跑（默认配置直接复用网格实验里已有的3轮均值数据，节省重跑）：

| 配置 | threshold_ratio | sync_window | 其余参数 |
|---|---|---|---|
| 默认 | 1.02 | 64 | min_prefill_tokens=256, max_swaps_per_layer=64 |
| 激进 | 1.01 | 32 | 同上 |
| 保守 | 1.10 | 128 | 同上 |

## 结果：三组参数在这3个负载上几乎没有区别

| 负载 | 默认 req/s | 激进 req/s | 保守 req/s | 默认TPOT | 激进TPOT | 保守TPOT |
|---|---|---|---|---|---|---|
| long_short | 47.28 | 47.27 | 46.92 | 1554.9 | 1539.8 | 1537.0 |
| short_short | 135.56 | 132.39 | 135.18 | 517.3 | 529.8 | 549.1 |
| long_long | 27.03 | 27.04 | 27.04 | 137.1 | 136.5 | 137.0 |

三组配置在三个负载上的差异全部在±3%以内，量级跟我们观察到的run-to-run噪声（同配置重复跑也有2-5%的波动）相当，**没有观察到任何一组参数明显优于默认**。

## Swap记录（激进/保守配置各自的DIAG日志）

**激进(threshold=1.01, sync_window=32)：**

| 负载 | 窗口 | avg_ratio_before | total_ops |
|---|---|---|---|
| long_short | 1-8 | 1.711→1.203 | 240,44,3,3,53,10,3,2 |
| short_short | (延续) | 1.192→1.202 | 5,48 |
| long_long | 未见新增窗口（继承前序状态已收敛） | - | - |

**保守(threshold=1.10, sync_window=128)：**

| 负载 | 窗口 | layers_touched | avg_ratio_before | total_ops |
|---|---|---|---|---|
| long_short+short_short | 1-3 | 91,93,85 | 1.711→1.224 | 240,75,16 |
| short_short(续)+long_long | 4-6 | 83,86,85,84 | 1.212→1.206 | 1,13,2,1 |

**关键观察**：保守配置下`layers_touched`明显低于默认/激进的94（分别是91/93/85/83/86/85/84，vs 激进和默认稳定在94）——**threshold=1.10确实让更多层"够不上修正门槛"被跳过**，符合参数本身的设计意图（阈值越高，只有更严重不均衡的层才会被处理）。但这个"少修一些层"的行为差异，**在这3个代表负载上没有转化成可观测的吞吐/延迟差异**。

## 结论

在Prover-V1这种**单一内容域**（都是Lean4数学证明，只是输入输出长度不同）的场景下，`threshold_ratio`（1.01-1.10范围内）和`sync_window`（32-128范围内）这两个参数的调整，**看不出对吞吐/延迟有实质影响**。推测原因：这类场景下收益的大头来自"冷启动那一次性修正"（把默认round-robin placement纠正到跟内容匹配），后续同域流量的漂移本身很小，参数调得更激进或更保守，能纠正的"边际不均衡"数量都很有限，自然看不出差异。

**更值得投入调参精力的场景是domain-switch类场景**（比如之前fixeddata/triplefixed实验里的Prover→BookCorpus切换）——那里我们已经验证过，`sync_window`的静态选择(32 vs 64)有真实、可观测的trade-off(sw32吞吐更高但TTFT更差)，`adaptive_window`机制在domain-switch场景下也拿到了真实的正收益。**单一内容域内部的静态参数微调，不如"检测到domain切换时临时调整"这类动态机制来得有效。**

## 局限

本次每组参数只跑了1次（n=1），而我们已经观察到同配置重复跑也有2-5%的自然波动，所以"三组参数没有区别"这个结论的置信度有限——真实差异如果小于噪声量级，用这个样本量是分辨不出来的。如果想要更确定的结论，需要每组配置至少3轮重复。

---

# 冷启动"蹭红利"公平性验证 (2026-07-22)

## 背景

用户提出疑虑：链式9负载测试里，只有排第一位的`short_short`真正独立扛了冷启动那次大修正的成本（P2P swap抢NVLink带宽），后面8个负载都是**继承已经收敛的placement**在跑,没再付出修正代价——而我们的负载顺序在3轮里从未变化(永远短→中→长)，"输入长度越长收益越大"这个结论有没有可能只是"排位越靠后蹭得越多"的假象？

## 验证方法

对全部9种负载，**每个负载单独起一次全新OEPLB服务器**（各自独立扛自己的冷启动，不继承任何前序状态），对比链式版本(3轮均值)的结果。Baseline因为从不做任何纠偏，链式/独立理论上不应有差异，作为对照组一并连测验证。

## 完整结果（9种负载全覆盖）

| 负载 | 链式BL | 独立BL | BL差异 | 链式OE | 独立OE | OE差异 | 链式收益% | 独立收益% |
|---|---|---|---|---|---|---|---|---|
| short_short | 121.56 | 122.71 | +0.9% | 135.56 | 137.45 | +1.4% | +11.5% | +12.0% |
| short_medium | 79.96 | 80.85 | +1.1% | 87.24 | 87.97 | +0.8% | +9.1% | +8.8% |
| short_long | 43.35 | 43.60 | +0.6% | 45.59 | 45.65 | +0.1% | +5.2% | +4.7% |
| medium_short | 85.58 | 85.01 | -0.7% | 97.03 | 98.89 | +1.9% | +13.4% | +16.3% |
| medium_medium | 62.31 | 62.53 | +0.4% | 69.63 | 69.33 | -0.4% | +11.7% | +10.9% |
| medium_long | 37.46 | 37.12 | -0.9% | 40.51 | 40.05 | -1.1% | +8.1% | +7.9% |
| long_short | 41.09 | 41.12 | +0.1% | 47.28 | 46.61 | -1.4% | +15.1% | +13.4% |
| long_medium | 34.19 | 34.33 | +0.4% | 38.96 | 38.84 | -0.3% | +14.0% | +13.1% |
| long_long | 24.50 | 24.72 | +0.9% | 27.03 | 26.95 | -0.3% | +10.3% | +9.0% |

**结论：冷启动"蹭红利"的担心基本不成立。** 9种负载里，Baseline的链式/独立差异全部在±1.1%以内（符合预期，baseline没有状态继承问题）；OEPLB的链式/独立差异全部在±1.9%以内，跟我们观察到的自然run-to-run噪声（2-5%）同量级或更小；最终的"OEPLB相对baseline的收益幅度"，链式版本和独立版本得出的排序趋势完全一致（**输入越长收益越大**这个结论在两种方法论下都稳定成立：short系（4.7-12.0%）< medium系（7.9-16.3%）< long系（9.0-15.1%，注意long系三者内部输出越长收益略降，但整体仍高于short系）），**不是排位效应造成的假象**。

## Swap记录：独立起服务器 vs 链式，过程差异巨大但结果趋同

**独立起服务器（9种负载各自的冷启动修正）：**

| 负载 | 总swap(独立) | 窗口数 | avg_ratio_before范围 |
|---|---|---|---|
| short_short | 240+44+110=394 | 3 | 1.182-1.711 |
| short_medium | 240+44+101+16+3=404 | 5 | 1.183-1.711 |
| short_long | 240+44+3+111+1=399 | 5 | 1.182-1.711 |
| medium_short | 240+44+46+2=332 | 4 | 1.195-1.711 |
| medium_medium | 240+44+47+5=336 | 4 | 1.196-1.711 |
| medium_long | 240+44+48=332 | 3+ | 1.220-1.711 |
| long_short | 240+44+52+10+1=347 | 5 | 1.205-1.711 |
| long_medium | 240+44+54+11+2+2=353 | 6 | 1.204-1.711 |
| long_long | 240+44+3+59-64+6-8+1-2+1-2 ≈ 350-360 (2轮均值) | 6-7 | 1.19-1.71 |

**链式（继承已收敛placement，仅需小幅touch-up）：**

| 负载 | 总swap(链式3轮均值) |
|---|---|
| short_short | ~320（3轮平均，因为它永远排第一，本身就相当于"独立"）|
| short_medium | ~1 |
| short_long | ~0.7 |
| medium_short | ~25 |
| medium_medium | ~3 |
| medium_long | 0 |
| long_short | ~61 |
| long_medium | ~17 |
| long_long | ~7 |

**观察**：独立起服务器时，除`short_short`本身（链式里也排第一，两者本来就该接近）外，其余8个负载的独立版本swap总量（330-400）比链式版本（0-61）高出一个数量级——**每次独立起服务器都要完整重付一次冷启动代价**，这是意料之中的。但正如上面吞吐结果显示的，**这个数量级的swap次数差异，最终换来的吞吐结果差异只有1-2个百分点**，进一步坐实了之前的判断：冷启动修正的核心价值在于"把默认round-robin纠正到匹配内容域"，一旦纠正到位，后续不管是小幅微调（链式）还是重新来一遍完整修正（独立），最终收敛的placement质量相近，性能表现也就相近。

## 对整份报告的最终定论

**本报告前面所有基于链式9负载方法论得出的结论（"输入越长收益越大"、"输出越长TPOT改善幅度变小"等）经过这次独立验证，均可信，不是冷启动位置偏差的产物。** 这次验证也顺便产出了一个额外发现：**独立起服务器时的swap总量是链式的10-15倍，但对最终性能的影响微乎其微**——如果以后要评估"降低swap频率/减少P2P开销"这类优化的价值，参照物应该是"链式/稳态"场景下的小幅swap量，不是冷启动那种极端情况下的swap量，否则容易高估优化空间。
